
<div class="row">
  <div class="col-md-9 col-lg-9">
    <h4> Please save your seed file </h4>
  </div>
  <div id="hint" class="col-md-9 col-lg-9" style="margin-top: 20px;">
    <button type="button" class="btn btn-default" title="Discovery Seed Instructions"
      data-container="body" data-toggle="popover" data-placement="right"
      data-content="Discovery Seed Format: <br>
      <ul><li> One entry per line  </li>
          <li> Comment line start with special char # </li>
          <li> The entry can be an URL: http://x.x.x.x/ </li>
          <li> The entry can be full-qualified domain name: www.yoursite.com </li>
          <li> The entry can be an IP address: x.x.x.x </li>
          <li> The entry can be a network block in IPv4 CIDR format: x.x.x.x/24  </li>
      </ul>
          "><span class="glyphicon glyphicon-question-sign"></span>Seed Format</button>
  </div>
</div>


<table class="table">
  <tr>
    <td>
      <div class="tab-content col-md-9 col-lg-9">
        <% data = String.new %>
        <% seeds = File.open(@file, 'r+') %>
        <% seeds.each_line { |line| data +=line }; seeds.close %>

          <%= form_tag "/seed/create" do %>
            <%= text_area_tag :file_tag, data, rows: 22, cols: 90 %>
            <%= hidden_field_tag :uid_tag, @uid %>
            <button class="btn btn-default" type="submit">Save</button>

          <% end %>

        </P>
      </div>
    </td>
  </tr>

</table>
